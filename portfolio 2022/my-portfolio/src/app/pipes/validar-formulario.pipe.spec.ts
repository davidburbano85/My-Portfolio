import { ValidarFormularioPipe } from './validar-formulario.pipe';

describe('ValidarFormularioPipe', () => {
  it('create an instance', () => {
    const pipe = new ValidarFormularioPipe();
    expect(pipe).toBeTruthy();
  });
});
