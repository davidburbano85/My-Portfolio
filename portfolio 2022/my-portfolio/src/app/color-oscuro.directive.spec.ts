import { ColorOscuroDirective } from './color-oscuro.directive';

describe('ColorOscuroDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOscuroDirective();
    expect(directive).toBeTruthy();
  });
});
